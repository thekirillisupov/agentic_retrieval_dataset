# Core (generation + verification against any OpenAI-compatible API or vLLM;
# httpx also powers the GigaChat embeddings retriever, numpy the index)
openai>=1.40.0
httpx>=0.27.0
numpy>=1.24
PyYAML>=6.0

# Optional: native Anthropic backend (llm.backend: anthropic)
# anthropic>=0.39.0

# Optional: hard-negative mining (negatives.enabled: true)
# sentence-transformers>=3.0.0

# Optional: pull MuSiQue from the HF hub for scripts/build_musique_dialogues.py
# (not needed if you pass --input with a local musique dump)
# datasets>=2.14.0

# Dev / tests
pytest>=7.0
